kind: Service
apiVersion: v1
metadata:
  labels:
    k8s-app: nginx-deployment
  name: frontend-service
  namespace: kube-system
spec:
  type: LoadBalancer
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
  selector:
    k8s-app: nginx