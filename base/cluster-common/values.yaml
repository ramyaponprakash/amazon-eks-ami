namespace: cluster-common
component: common
env:

awsAuth:
  boostrap:
    roleArn:
  pipeline:
    roleArn:
  eksConsoleDashboard:
    roleArn:

caArn:

secretstore:
  enabled: true
  serviceAccount:
    name: secrets-access-sa

# For conditional release per env
cert-manager:
  enabled: true

cluster-autoscaler:
  enabled: true

external-dns:
  enabled: true

external-secrets:
  enabled: true