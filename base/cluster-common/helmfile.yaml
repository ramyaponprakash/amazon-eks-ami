helmBinary: {{ requiredEnv "HELM_BINARY" }}

environments:
  dev:
    values:
      - ./values.yaml
      - ../../dev/global.yaml.gotmpl
      - ../../dev/cluster-common/values.yaml
  qa:
    values:
      - ./values.yaml
      - ../../dev/global.yaml.gotmpl
      - ../../dev/cluster-common/values.yaml

repositories:
  - name: cluster-common
    url: s3://sdx-eks-artifacts/charts/cluster-common
  - name: external-secrets
    url: https://charts.external-secrets.io
  - name: cluster-autoscaler
    url: https://kubernetes.github.io/autoscaler
  - name: cert-manager
    url: https://charts.jetstack.io
  - name: cert-manager-awspca
    url: https://cert-manager.github.io/aws-privateca-issuer

missingFileHandler: Error

releases:
  - name: cluster-common
    namespace: cluster-common
    createNamespace: true
    chart: cluster-common/cluster-common
    version: {{ .Values.chartVer | quote }}
    missingFileHandler: Warn
    disableValidationOnInstall: true # CRD
    values:
      - ./values.yaml
      - ./values.yaml.gotmpl
      - ../../{{ .Environment.Name }}/cluster-common/values.yaml
      - ../../{{ .Environment.Name }}/cluster-common/values.yaml.gotmpl
    needs:
      - external-secrets
      - cert-manager-awspca
    hooks:
      - events: [ "presync" ]
        showlogs: true
        command: "./scripts/cluster-pre.sh"
        args:
          - "-c"
          - "{{`{{ .Values.clusterName }}`}}"
          - "-n"
          - "{{`{{ .Values.namespace }}`}}"
      - events: [ "presync" ]
        showlogs: true
        command: "./scripts/ebscsi-pre.sh"
        args:
          - "-c"
          - "{{`{{ .Values.clusterName }}`}}"
          - "-n"
          - "{{`{{ .Values.namespace }}`}}"

  - name: external-secrets
    namespace: cluster-common
    createNamespace: true
    chart: external-secrets/external-secrets
    version: 0.5.8
    missingFileHandler: Warn
    condition: external-secrets.enabled
    disableValidationOnInstall: true # CRD
    values:
      - ./values.external-secrets.yaml
      - ./values.external-secrets.yaml.gotmpl
      - ../../{{ .Environment.Name }}/cluster-common/values.external-secrets.yaml
      - ../../{{ .Environment.Name }}/cluster-common/values.external-secrets.yaml.gotmpl
    hooks:
      - events: [ "presync" ]
        showlogs: true
        command: "./scripts/secretaccess-pre.sh"
        args:
          - "-a"
          - "{{`{{ .Values.accountId }}`}}"
          - "-e"
          - "{{`{{ .Environment.Name }}`}}"
          - "-c"
          - "{{`{{ .Values.clusterName }}`}}"
          - "-n"
          - "{{`{{ .Values.namespace }}`}}"
      - events: [ "postsync" ]
        showlogs: true
        command: "sleep"
        args:
          - "45"

  - name: cluster-autoscaler
    namespace: cluster-common
    createNamespace: true
    chart: cluster-autoscaler/cluster-autoscaler
    version: 9.19.2
    missingFileHandler: Warn
    condition: cluster-autoscaler.enabled
    disableValidationOnInstall: true # CRD
    needs:
      - cluster-common
    values:
      - ./values.cluster-autoscaler.yaml
      - ./values.cluster-autoscaler.yaml.gotmpl
      - ../../{{ .Environment.Name }}/cluster-common/values.cluster-autoscaler.yaml
      - ../../{{ .Environment.Name }}/cluster-common/values.cluster-autoscaler.yaml.gotmpl
    hooks:
      - events: ["presync"]
        showlogs: true
        command: "./scripts/clusterautoscaler-pre.sh"
        args:
          - "-a"
          - "{{`{{ .Values.accountId }}`}}"
          - "-e"
          - "{{`{{ .Environment.Name }}`}}"
          - "-c"
          - "{{`{{ .Values.clusterName }}`}}"
          - "-n"
          - "{{`{{ .Values.namespace }}`}}"
      - events: ["postsync"]
        showlogs: true
        command: "./scripts/clusterautoscaler-post.sh"
        args:
          - "-a"
          - "{{`{{ .Values.accountId }}`}}"
          - "-e"
          - "{{`{{ .Environment.Name }}`}}"
          - "-c"
          - "{{`{{ .Values.clusterName }}`}}"
          - "-n"
          - "{{`{{ .Values.namespace }}`}}"

  - name: cert-manager
    namespace: cluster-common
    createNamespace: true
    chart: cert-manager/cert-manager
    version: 1.9.1
    missingFileHandler: Warn
    condition: cert-manager.enabled
    disableValidationOnInstall: true # CRD
    values:
      - ./values.cert-manager.yaml
      - ./values.cert-manager.yaml.gotmpl
      - ../../{{ .Environment.Name }}/cluster-common/values.cert-manager.yaml
      - ../../{{ .Environment.Name }}/cluster-common/values.cert-manager.yaml.gotmpl

  - name: cert-manager-awspca
    namespace: cluster-common
    createNamespace: true
    chart: cert-manager-awspca/aws-privateca-issuer
    version: 1.2.0
    missingFileHandler: Warn
    condition: cert-manager.enabled
    disableValidationOnInstall: true # CRD
    needs:
      - cert-manager
    values:
      - ./values.cert-manager-acm.yaml
      - ./values.cert-manager-acm.yaml.gotmpl
      - ../../{{ .Environment.Name }}/cluster-common/values.cert-manager-acm.yaml
      - ../../{{ .Environment.Name }}/cluster-common/values.cert-manager-acm.yaml.gotmpl
    hooks:
      - events: [ "presync" ]
        showlogs: true
        command: "./scripts/acmpca-pre.sh"
        args:
          - "-a"
          - "{{`{{ .Values.accountId }}`}}"
          - "-e"
          - "{{`{{ .Environment.Name }}`}}"
          - "-c"
          - "{{`{{ .Values.clusterName }}`}}"
          - "-n"
          - "{{`{{ .Values.namespace }}`}}"
