env: dev

vpcId: vpc-09a58d6d
accountId: 342446142760

# chart installing conditions for multi-envs
external-alb-default:
  enabled: true # enabled, portal/API access without FW

external-nlb-default:
  enabled: false # disabled, external NLB until we have any TCP servers to test

internal-alb-default:
  enabled: false

internal-nlb-default:
  enabled: true # QA cross zone API, NATS access